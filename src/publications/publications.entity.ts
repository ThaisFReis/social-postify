import { Publication } from '../publication/entities/publication/publication';

describe('Publication', () => {
  it('should be defined', () => {
    expect(new Publication()).toBeDefined();
  });
});
